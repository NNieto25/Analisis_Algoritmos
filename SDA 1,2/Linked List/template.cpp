#include "LinkedList.h"
#include <iostream>

using namespace std;

int main(){
    node *pList = nullptr;
    push_back(T(1),&pList);
    push_back(T(2),&pList);
    push_back(T(3),&pList);
    push_back(T(4),&pList);

    return 0;
}